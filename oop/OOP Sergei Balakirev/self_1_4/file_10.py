class Translator:

    def add(self, eng, rus):
        new_dict = []
        for _ in new_dict:
            if eng not in  new_dict:
                new_dict.append(dict(zip(eng.split(), rus.split())))
            elif







        return new_dict

a = Translator()
a.add('Hellow', 'привет')

